[project]
name = "ensemble-bpe-paper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "datasets>=4.3.0",
    "matplotlib>=3.10.7",
    "psutil>=7.1.2",
    "rich>=14.2.0",
    "tiktoken>=0.12.0",
    "tokenizers>=0.22.1",
    "transformers>=4.57.1",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "ruff>=0.14.3",
]
